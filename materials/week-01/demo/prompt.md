# The live chatbot moments — session 1

1. (After the folder-tree segment) Ask, verbatim:
   "Where did my file ledger.csv go?"
   Point at the confident guess. Rule on screen: the model sees exactly
   what you paste, nothing else.

2. (Chat vs harness, watched) Same task both ways:
   "Write a Python line that prints the first line of ledger.csv."
   - Chat: paste answer into editor, run it ourselves.
   - Harness (professor's machine): same ask; it reads the folder, runs,
     shows output. Name both. Two minutes total, no deep dive.
